.venv/
__pycache__/
*.pyc

# Redacted outputs are generated, never commit them
*_redacted.pdf

# Ignore stray PDFs at the repo root, but keep the demo sample under samples/
/*.pdf
!samples/sample.pdf
